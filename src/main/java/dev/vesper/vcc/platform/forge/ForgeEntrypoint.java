package dev.vesper.vcc.platform.forge;

//? forge {

/*import dev.vesper.vcc.Config;import dev.vesper.vcc.VCC;
import net.minecraftforge.fml.ModLoadingContext;import net.minecraftforge.fml.common.Mod;import net.minecraftforge.fml.config.ModConfig;

@Mod(VCC.MOD_ID)
public class ForgeEntrypoint {

	public ForgeEntrypoint() {
		VCC.onInitialize();
		ModLoadingContext.get().registerConfig(ModConfig.Type.CLIENT, Config.CONFIG_SPEC);
	}
}
*///?}
