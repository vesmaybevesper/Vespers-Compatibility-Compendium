package dev.vesper.vcc.util;

public class GlobalRipple {
/*
	public void spawn(Level level, Vec3 pos) {
		if (Config.rippleType == Config.RippleType.EFFECTIVE){
			if (EffectiveUtils.isGlowingWater(level, pos)) {
				level.addParticle(EffectiveParticles.GLOW_RIPPLE, pos.x(), pos.y(), pos.z(), 0.0D, 0.0D, 0.0D);
			} else {
				level.addParticle(EffectiveParticles.RIPPLE, pos.x(), pos.y(), pos.z(), 0.0D, 0.0D, 0.0D);
			}
		} else if (Config.rippleType == Config.RippleType.PARTICLE_RAIN){
			level.addParticle(ParticleRain.RIPPLE, pos.x(), pos.y(), pos.z(), 0.0D, 0.0D, 0.0D);
		}
	}

	public void spawn(SpawnReason reason, Level level, Vec3 pos) {
		if (reason == SpawnReason.PARTICLE) {

		} else if (reason == SpawnReason.RAIN) {

		}
	}

	public enum SpawnReason {PARTICLE, RAIN}*/
}
