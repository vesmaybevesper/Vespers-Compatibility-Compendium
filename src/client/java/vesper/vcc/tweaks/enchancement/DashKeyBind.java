/*package vesper.vcc.tweaks.enchancement;

import moriyashiine.enchancement.client.EnchancementClient;
import net.fabricmc.loader.api.FabricLoader;
import net.minecraft.client.MinecraftClient;
import vesper.vcc.Config;
import vesper.vcc.utils.Util;

public class DashKeyBind {
    public static void init(){
        if (FabricLoader.getInstance().isModLoaded("enchancement")){
            if (Config.EnchanceKeyBindFix) {
                MinecraftClient client = MinecraftClient.getInstance();
                if (client.options != null) {
                    if (MinecraftClient.getInstance().options.sprintKey == null) {
                        Util.log("Fixing Enchancement key bind bug");

                    }
                }
            }
        }
    }
}*/
